import React from 'react';
import { render } from '@testing-library/react';
import Logo from './Logo'; 

test('Logo component renders without errors', () => {
  const { container } = render(<Logo />);Í

});
